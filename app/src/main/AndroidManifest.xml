<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a_citra.belajar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sign_in">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Pre Stand UP"
            android:theme="@style/AppTheme">
            <activity android:name=".tab_fragment_buat_janji" />
            <activity android:name=".tab_fragment_lapor" />
            <activity android:name=".tab_fragment_profil" />
            <activity android:name=".DatePickFragment"></activity>
        </activity>

    </application>

</manifest>